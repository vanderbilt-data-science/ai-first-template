# ML Experiment Tracker

Track fine-tuning experiments and compare model performance.

## Setup
```bash
pip install -r requirements.txt
```

## Usage
```bash
python tracker.py
```

Results live in `results/experiments.csv`. Add rows there as experiments complete.

See `CLAUDE.md` for full project context.
